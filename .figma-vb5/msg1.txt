visits: show real labels in selects, fix the reports bars and fit each board in one viewport

Base UI Select needs `items` on the root, so the branch pickers on Calendar,
Slot availability and Reports rendered raw UUIDs or `all`, and the slot config
showed `30` instead of `30 minutes`. Pass `items` everywhere.

The weekly bar heights were percentages inside an auto-height flex column, so
they collapsed and the chart looked empty; give the track a definite height.
Compact the day board to 38px rows with a sticky header so a full 09:00-17:00
day fits a 14-inch laptop, and stop the check-in actions wrapping onto a second
line. Column, date, plural and title wording now follows Figma.
